import { UserContext } from '../lib/context';

export default function Page({ }) {

    const { user } = useContext(UserContext)

    return (
        <main>
            <div>
                
            </div>
        </main>
    )
}